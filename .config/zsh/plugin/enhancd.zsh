# `cd ..` / `cd .` / `cd`(引数なし)で fzf 選択画面を出さず、
# builtin cd と同じ挙動にする(変数名は enhancd 2.5.x のもの)
ENHANCD_ENABLE_DOUBLE_DOT=false
ENHANCD_ENABLE_SINGLE_DOT=false
ENHANCD_ENABLE_HOME=false
ENHANCD_HYPHEN_NUM=20
