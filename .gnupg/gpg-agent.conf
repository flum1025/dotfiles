# pinentry-mac を使い、パスフレーズを macOS キーチェーンへ保存できるようにする。
# 初回だけ入力し「Save in Keychain」にチェックすれば、以降 commit 署名で聞かれなくなる。
pinentry-program /opt/homebrew/bin/pinentry-mac

# エージェントのキャッシュ保持時間(秒)。400 日 = 実質セッションをまたいでも保持。
# キーチェーン保存と併用することで再入力をほぼ不要にする。
default-cache-ttl 34560000
max-cache-ttl 34560000
