# Terraform CLI 設定。複数リポジトリでプロバイダを一元キャッシュし重複DLを避ける。
# 対応FS(macOS/Linux)では各 .terraform/providers 配下がこのキャッシュへの symlink になる。
# 注意: このディレクトリは事前に作成が必要(terraform は自動作成しない)。
# 再生成可能なキャッシュなので XDG 準拠で ~/.cache 配下に置く($HOME のみ展開される)。
plugin_cache_dir = "$HOME/.cache/terraform/plugin-cache"
